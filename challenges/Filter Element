/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var filter = function(arr, fn) {
    var array1 =[];
    for (var i =0 ; i<arr.length; i++){
        if(fn(arr[i],i)){
        array1.push(arr[i]);
    }}
    return array1;
};
